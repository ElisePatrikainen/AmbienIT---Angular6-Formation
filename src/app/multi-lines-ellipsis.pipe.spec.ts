import { MultiLinesEllipsisPipe } from './multi-lines-ellipsis.pipe';

describe('MultiLinesEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiLinesEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
