import { OnScrollFadeInDirective } from './on-scroll-fade-in.directive';

describe('OnScrollFadeInDirective', () => {
  it('should create an instance', () => {
    const directive = new OnScrollFadeInDirective();
    expect(directive).toBeTruthy();
  });
});
